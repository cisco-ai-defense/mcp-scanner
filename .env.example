# MCP Scanner SDK Configuration
# Copy this file to .env and update the values

# Cisco AI Defense API Key (required for API analyzer)
MCP_SCANNER_API_KEY="..."

# API Endpoint URL - defaults to US production if not specified
MCP_SCANNER_ENDPOINT="https://us.api.inspect.aidefense.security.cisco.com/api/v1"

# LLM Analyzer Configuration
MCP_SCANNER_LLM_API_KEY="sk..."
# MCP_SCANNER_LLM_MODEL="gpt-4o"
# MCP_SCANNER_LLM_BASE_URL=""
# MCP_SCANNER_LLM_API_VERSION=""

# Meta-Analyzer Configuration (optional - falls back to LLM settings if not set)
# The meta-analyzer performs second-pass analysis on findings from multiple analyzers
# to prune false positives, prioritize findings, and provide actionable recommendations.
# Enable via CLI with --enable-meta flag. Requires minimum 2 analyzers.
#
# MCP_SCANNER_META_LLM_API_KEY=""     # Falls back to MCP_SCANNER_LLM_API_KEY
# MCP_SCANNER_META_LLM_MODEL=""       # Falls back to MCP_SCANNER_LLM_MODEL
# MCP_SCANNER_META_LLM_BASE_URL=""    # Falls back to MCP_SCANNER_LLM_BASE_URL
# MCP_SCANNER_META_LLM_API_VERSION="" # Falls back to MCP_SCANNER_LLM_API_VERSION
